# Base de données PostgreSQL (optionnel — sans cette variable, le stockage en mémoire est utilisé)
DATABASE_URL=postgresql://langue:langue@localhost:5432/langueauchat

# Groq API pour la validation IA des mots et noms (optionnel — fallback sur validation basique)
GROQ_API_KEY=
